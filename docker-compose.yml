# Local development — hot reload via Vite (no Docker required)
# Production uses docker-compose.prod.yml on the VPS.

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3010:3000"
    env_file:
      - .env
