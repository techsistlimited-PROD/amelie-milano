import type { ReactNode } from "react";

/** Builder visual override removed — static React pages + CMS data overlays remain. */
const BuilderRouteContent = ({ children }: { children: ReactNode }) => <>{children}</>;

export default BuilderRouteContent;
